# FUTURE IDEAS (НЕ реализовывать без отдельной сессии)

Записано по итогам `feature/v1.25-personal-final`. Personal Edition считается
законченным после S24 acceptance; ниже — только backlog.

## Инструменты

- QR generate/scan (нужна лёгкая зависимость, напр. ZXing — оценить вес APK)
- Больше deterministic-утилит только по реальному запросу владельца
- Экспорт VibeCode-файлов напрямую в файлы (сейчас copy/share)
- P0: стриминг чанков переводчика по мере готовности (сейчас — последовательно, итог в конце)

## Агент

- Подтверждение (confirm) для действий с побочными эффектами перед выполнением
- Опциональные destructive-операции только с явным confirm-диалогом
- Больше action-типов только по запросу (сейчас 10 достаточно)

## Соцсеть

- LOCAL QUEUE режим автопостинга (local LLM готовит drafts при открытом приложении)
- Реакции помимо лайков — только если захочется
- НЕ делать: публичная соцсеть, сервер, аккаунты, simulation engine

## Платформа

- Desktop-версия — НЕ делать (вне scope Personal Edition)
- Cloud backend / sync — НЕ делать (local-first принцип)
- Plugin system / multi-agent framework / vector DB — НЕ делать
- Убрать `release.keystore` + `keystore.properties` из git (нужна ротация ключа + решение Lead'а)
- Убрать старые `NeuroPocket-v*.apk` из рабочей папки в релизы GitHub (сейчас лежат рядом с кодом, в git не входят)

## Модели/рантаймы

- Vulkan GPU-слой для llama (эксперимент, только после стабильности CPU)
- Новые модели — только по запросу владельца
